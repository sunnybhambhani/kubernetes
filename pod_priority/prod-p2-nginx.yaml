apiVersion: v1
kind: Pod
metadata:
  name: pd-nginx
  labels:
    env: pd
spec:
  containers:
  - name: pd-nginx
    image: nginx
    resources:
      requests:
        memory: "2Gi"
        cpu: "2"
      limits:
        memory: "2Gi"
        cpu: "2"
  priorityClassName: prod-pc-p2
